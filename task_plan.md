# タスク計画（book-notes-app）

当初計画: 24タスク（子エージェント担当）＋ 親AI担当3件 ＋ 人間確認1件

`（親AI担当）` は子エージェントではなく親AI/人間が外部システムに対して直接行う作業。子タスクが全て完了しても、これらが未完了ならプロジェクトは完了とみなさない。

（2026-07-16 独立監査を受けて、旧タスク19・20・12を分割し、相関図の描画方法をd3-forceに確定、環境変数リストにSupabaseのanon key/service role keyの両方を明記するよう修正）

## 環境構築・初期化

- [ ] タスク1: Next.js（App Router、TypeScript）プロジェクトの初期化。検証: `npm run build`成功、`npm run dev`後にトップページへのfetchで200
- [ ] タスク2（親AI担当）: Supabaseプロジェクトを作成し、接続情報（URL・anon key・service role key）を取得する
- [ ] タスク3: Supabaseクライアント初期化コード（`lib/supabase.ts`）と`.env.local.example`の作成。検証: `npx tsc --noEmit`が通る

## データ層

- [ ] タスク4: `books`・`book_links`テーブルのマイグレーションSQL作成。検証: SQL構文チェック
- [ ] タスク5（親AI担当）: マイグレーションSQLを実際のSupabaseプロジェクトに適用する
- [ ] タスク6: データアクセス関数群（`lib/db/books.ts`, `lib/db/links.ts`）の実装。検証: CRUD操作のユニットテスト

## ロジック層

- [ ] タスク7: パスワード認証ロジック（`lib/auth.ts`: 比較・署名付きCookie発行/検証）の実装。検証: 正誤パスワードでの単体テスト
- [ ] タスク8: 認証middlewareの実装（未ログイン時は`/login`へリダイレクト、`/login`と`/api/login`は除外）。検証: cookie有無での挙動テスト
- [ ] タスク9: NDL Searchでの書籍キーワード検索ロジック（`lib/bookSearch.ts`）の実装。検証: 既知の書名での検索結果取得テスト
- [ ] タスク10: openBDでの表紙画像・出版情報取得ロジック（`lib/openbd.ts`）の実装。検証: 既知のISBNでの取得テスト
- [ ] タスク11: Claude APIを使ったおすすめ本生成ロジック（`lib/recommend.ts`）の実装。検証: モックデータでのプロンプト組み立て・レスポンス解析の単体テスト（実APIキーは未設定の前提）

## API/インターフェース層

- [ ] タスク12: 本の一覧取得・新規作成APIルート（`app/api/books/route.ts`: GET一覧, POST作成）の実装。検証: fetchでのステータス・内容確認
- [ ] タスク13: 本の詳細取得・更新・削除APIルート（`app/api/books/[id]/route.ts`: GET詳細, PUT更新, DELETE削除）の実装。検証: 同上（タスク12のPOSTで作成したレコードに対してGET/PUT/DELETEを試す）
- [ ] タスク14: リンクCRUD APIルート（`app/api/links/route.ts`: 作成・削除）の実装。検証: 同上
- [ ] タスク15: 書籍検索APIルート（`app/api/book-search/route.ts`）の実装。検証: 同上
- [ ] タスク16: おすすめ本APIルート（`app/api/recommend/route.ts`）の実装。検証: モック経由でのエラーハンドリング確認
- [ ] タスク17: ログイン/ログアウトAPIルート（`app/api/login/route.ts`, `app/api/logout/route.ts`）の実装。検証: 正誤パスワード・ログアウト後の再アクセス確認

## UI層

- [ ] タスク18: ログイン画面（`app/login/page.tsx`）の実装。検証: フォーム送信・成功時リダイレクトのコードレビュー＋API疎通確認
- [ ] タスク19: 本の一覧画面（`app/page.tsx`）の実装（モバイルレスポンシブ、タップ領域44px以上）。検証: `/api/books`のGETを呼び出し一覧が表示される導線をコードレビュー＋API疎通確認で確認。主要ブレークポイント（スマホ幅・PC幅）でのレイアウト崩れが無いことをコードレビューで確認
- [ ] タスク20: 書籍検索・候補選択UI（検索ボックス→NDL Search候補一覧の表示→候補選択）の実装。選択結果は次タスクのフォームに渡せる形（親コンポーネントの状態、またはURLクエリ等）にする。検証: `/api/book-search`への疎通確認、候補選択時に選択結果が正しく受け渡される導線をコードレビューで確認
- [ ] タスク21: 新規登録フォーム本体（タスク20の選択結果を初期値として受け取り、評価・要約・なぜ刺さったか等の記録項目を入力、Zodによる入力直後バリデーション、保存処理呼び出し）の実装。検証: 不正な入力値（未評価、規定文字数超過等）がバリデーションで弾かれるユニットテスト、保存処理が`/api/books`のPOSTを正しく呼び出すことをコードレビュー＋API疎通確認で確認
- [ ] タスク22: 本の詳細・編集画面（記録内容の表示、編集フォームでの保存、本自体の削除ボタン）の実装。検証: `/api/books/[id]`のGET/PUT/DELETEへの疎通確認、削除ボタン押下時の確認導線をコードレビューで確認
- [ ] タスク23: 本同士のリンク管理UI（関連本の追加・解除。詳細画面から呼び出す独立したコンポーネント）の実装。検証: `/api/links`への疎通確認をコードレビューで確認
- [ ] タスク24: 相関図画面（`app/graph/page.tsx`、d3-forceでノード配置を計算しSVGで描画）の実装。検証: サンプルデータ（本数冊分のノード・リンク）でd3-forceのシミュレーション結果（ノード座標・エッジ対応）が期待通りであることのユニットテスト、描画コードのレビュー
- [ ] タスク25: おすすめ本画面（`app/recommend/page.tsx`、非同期処理中のボタン非活性化・ローディング表示）の実装。検証: `/api/recommend`への疎通確認、非同期処理中はボタンがdisabledになりローディング表示が出ることをコードレビューで確認

## 結合テスト・仕上げ

- [ ] タスク26: ヘッドレス起動確認（`npm run build && npm run start`後、主要ページ・APIルートへの疎通確認）
- [ ] タスク27（親AI担当）: Vercelへのデプロイ設定（GitHub連携、環境変数 `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` / `APP_PASSWORD` / `ANTHROPIC_API_KEY` の設定）
- [ ] タスク28（人間による確認待ち）: 公開URLへの実アクセスで、ログイン→本の登録→一覧表示という一連のデータを伴う操作を試してもらう
