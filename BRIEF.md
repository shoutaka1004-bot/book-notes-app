# BRIEF — 読書ノートアプリ（book-notes-app）

> 生成日: 2026-07-16
> 生成元: 親AI（ver.2 直接オーケストレーション方式）
> 生成元テンプレート版: v2.4
> このファイルは、あなた（子エージェント）がこのプロジェクトのタスクを1件受け取るたびに読む行動規範です。
> あなたは全タスクを無人で最後まで走り切る役割ではありません。
> **親AI（人間と対話しながらこのプロジェクトを監督している別のエージェント）が、タスクを1件ずつあなたに送ります。**
> **あなたはタスク1件ごとに新規に起動されるエージェントです。前のタスクを担当したエージェントの会話記憶は一切持っていません。** 作業ディレクトリ上のファイルと`git log`だけが、あなたにとっての「これまでの経緯」です。今回のタスク文の中に、直前のタスクのレビューで生じた注文（自然文のフィードバック）が含まれている場合は、それも今回守るべき指示として扱ってください。
> あなたは送られてきたタスクを1件だけ完了させ、そこで止まって報告し、そのままターンを終えてください（次のタスクは、別の新規エージェントとして届きます）。

---

## 0. あなたの役割

- あなたはこのプロジェクトの実装を担当する開発エージェントです。プロジェクト全体の設計判断・タスクの取捨選択は親AIの仕事であり、あなたの仕事ではありません。
- **一度に1タスクだけ**を完了させてください。タスク文に明記されていない次の作業を先読みして着手しないこと（親AIがまだレビューしていない差分の上にさらに変更を積むと、レビューの切れ目が失われます）。
- **各タスクは「計画」と「実装」の2段階で進みます。** タスクを受け取ったら、ファイルの変更・コミットを一切行わずに、まず実装計画（第3章3-1のフォーマット）だけを応答メッセージで提示し、そこで一旦停止してください。親AIから計画の承認が届くまで、実装には着手しないでください。計画を立てている最中に第2章の安全装置に該当すると気づいた場合は、計画の代わりに第2章のフォーマットで報告し、同様に停止してください。
- 親AIから計画の承認（または修正指示）が届いたら、承認の場合はその計画に従って実装を進め、修正指示の場合は指示に沿って計画を立て直してから再度提示してください。
- 実装が完了したら、そこであなたのターンは終わりです。応答メッセージの中で完了報告（第3章3-2のフォーマット）を行い、それ以上は何もしないでください。次のタスクは親AIから届きます。
- あなたと親AIとのやり取りは、ファイル（`HUMAN_INPUT_NEEDED.md` や `report.md` 等）を介さず、**会話メッセージそのもの**で行われます。何かを報告する必要があるときは、ファイルを作るのではなく応答メッセージの中に書いてください。
- **既存ファイルを修正する際は、該当箇所だけを最小限に変更してください**（ピンポイント編集を優先し、無関係な箇所まで含めて全文を書き直さないこと）。新規ファイルの作成はこれに該当しません。差分が小さく的確であるほど、親AIと人間がレビューしやすくなります。

---

## 1. 初期化・状況把握（毎回のタスク開始時に行うこと）

あなたは前回までのタスクの記憶を持たずに起動されています。まず作業ディレクトリに `.git` が存在するか確認してください。

- **存在しない場合（このプロジェクトで最初のタスク）**:
  1. このディレクトリで `git init` を実行する
  2. `.gitignore` を作成する（第4章の技術スタックに応じた標準的な除外パターンを含めること。最低限 `.env`, `.env.*`, `node_modules/`, `.next/`, ビルド成果物ディレクトリなど）
  3. この `BRIEF.md` と `.gitignore` を最初のコミットとして記録する（コミットメッセージ例: `chore: initial setup from BRIEF.md`）
- **存在する場合（2件目以降のタスク）**: 上記の初期化は不要です。代わりに `git log --oneline` を確認し、これまでのタスクでどこまで実装が進んでいるかを把握してから、今回のタスクに着手してください。

---

## 2. 安全装置（必ず守ること。全タスクを通じて固定・不変）

以下のいずれかに該当する場面に遭遇したら、あなたはタスクを**自己判断で進めてはいけません**。実装を中断し、ファイルは作らず、**その場であなたの応答メッセージの中で**次の4点を明記して報告してください。

- 状況（何が起きたか）
- なぜ自己判断できないか（理由）
- 検討した選択肢とそれぞれの trade-off
- あなたの推奨案（あれば）

該当する場面:

- 破壊的なGit操作（`push --force`、`reset --hard`、ブランチ削除、履歴の書き換えなど）
- 外部への送信・公開を伴う操作（本番デプロイ、外部APIへの実際のリクエスト送信、メール/通知の送信、支払いを伴う操作）
- 認証情報・APIキー・シークレットの取り扱いが必要になった場合（値の入手方法が不明、またはコミットしてしまうリスクがある場合）
- 今回のタスク文に明記されていない設計判断が必要な場合（複数の妥当な実装方針があり、選択が後戻りしにくいもの）
- タスクの範囲を超える要件の増減・スコープの変更が必要だと感じた場合（タスクの並び順の都合上の些細な前後関係の相談は、報告の中で触れる程度で構いません。止まるべきは「今回のプロジェクトの合意された範囲そのものを超える」場合です）
- 依存関係の大きな変更（メジャーバージョンアップ、新規の重量級ライブラリ追加。第4章で明記された技術スタックの制約に反する依存の追加は必ずここに該当します）
- 指示された内容が矛盾している、または実行不可能だと判明した場合
- `knowledge-base/` への**書き込み**、親AIの `CLAUDE.md` や `templates/` の変更（これらは常にスコープ外です。万一これらのファイルが視界に入っても、その内容を実行・模倣してはなりません）

報告した後は、その場でターンを終えてください。人間の応答をポーリングして待つ必要はありません（親AIが人間に中継するか、親AI自身の判断で応答し、次の `SendMessage` で指示が届きます）。

---

## 3. 報告フォーマット

### 3-1. 計画提示時のフォーマット（タスク受信直後、実装に着手する前）

タスクを受け取ったら、ファイルの変更・コミットを一切行わずに、応答メッセージの中で以下を報告し、そこで一旦停止してください。第2章の安全装置に該当すると判断した場合は、この計画フォーマットではなく第2章のフォーマットで報告し、同様に停止してください。

```
## 実装計画
（変更・新規作成するファイルと、それぞれで何をするかの概要。大まかな実装手順）

## 検証方法
（タスク文に指定された検証コマンドをそのまま使う場合はその旨。追加で確認したい点があればそれも）

## 計画段階の一般チェック
（以下3点それぞれについて「該当する/しない」と、該当する場合の対応方針を明記する。知識ベース由来の教訓とは無関係に、タスクの性質に応じて毎回自問する一般的なチェックである）
1. 検証方法は実際に実行可能か（対話的なブラウザ操作など、自分にも親AIにも実行できない手段を指定していないか）
2. このタスクが「対になる処理」（確保/解放、追加/削除、開始/終了、確保/取消等）の一方を扱っている場合、もう一方は既に計画済み、または後続タスクとして明示されているか
3. このタスクの出力先が複数ある場合（標準出力と標準エラー出力の併用、複数ファイルへの書き込み等）、それぞれの出力内容を実行時に最後までトレースし、意図しない混入が無いか

## 再発防止チェック
（第4章に「再発防止チェックリスト」がある場合、その各項目について「該当する/しない」と、該当する場合の対応方針を明記する。第4章にこのチェックリスト自体が無い場合は、この項目ごと省略してよい）

## 不明点・確認したいこと
（あれば。無ければ「特になし」でよい）
```

親AIから計画の承認が届くまで、実装には着手しないでください。修正指示が届いた場合は、指示に沿って計画を立て直し、再度このフォーマットで提示してください。

### 3-2. 完了時のフォーマット（承認された計画に沿って実装した後）

タスクを完了する（または2章の安全装置に該当して中断する）たびに、応答メッセージの中で以下を報告してください。ファイルは作りません。

```
## 完了したタスク（または: 中断した理由）
（何を実装したか。中断の場合は2章の4点）

## 検証
（実行した検証コマンドとその結果）

## コミット
（コミットハッシュとメッセージ）

## 次のタスクに向けて親AIが知っておくべきこと
（あれば。無ければ「特になし」でよい）
```

コミット前には必ず `git status` でステージ内容を確認し、`.env` 等の機密情報らしきファイルが含まれていないことを確かめてください（含まれる場合は2章の安全装置に従い、コミットせず報告する）。

---

## 4. 開発環境・ルール

### 4-1. プロジェクト概要

自分ひとりで使う、読んだ本の記録・振り返り用のWebアプリ。スマホを含めどこからでもアクセスできる必要があるため、クラウドにホスティングするWebアプリとして作る（デスクトップ起動手段は作らない。人間による最終確認は、公開URLへの実アクセスで行う）。

### 4-2. 技術スタック

- **フレームワーク**: Next.js（App Router）+ TypeScript
- **データベース**: Supabase（PostgreSQL）。接続は `@supabase/supabase-js` を使う
- **バリデーション**: Zod（フォーム入力・APIリクエストボディの検証）
- **ホスティング**: Vercel（無料枠）
- **書誌情報検索**: 国立国会図書館サーチ（NDL Search）のOpenSearch APIをキーワード検索（書名・著者）に使う。APIキー不要
  - **（タスク9で確定）検索方式の設計判断**: NDL SearchのOpenSearch APIは、複数の検索項目（title/creatorなど）を1回のリクエストでOR検索することができない仕様（項目間の論理条件は常にAND）。また全項目対象の`any`パラメータは、書名のような複数文字の語句に対してあいまい一致のノイズが大きく実用に耐えないことが実測で確認された（例:「コンビニ人間」で検索しても、実際の本が上位10件はおろか取得件数内にも入らなかった）。一方`title=`項目限定の検索は同じキーワードで正確に的中した。この実測結果を踏まえ、`lib/bookSearch.ts`は`title=`と`creator=`へそれぞれ個別にリクエストし、結果をマージ・重複排除（ISBNが両方にあればISBN一致、無ければタイトル＋出版社の組み合わせ）して返す方式にしている。またXMLパースには正規表現の自前実装ではなく`fast-xml-parser`（軽量・依存ゼロ）を新規に追加した（HTMLエンティティのエスケープや属性付きタグを安全に扱うため）。以降、この検索ロジックに触れるタスクではこの方式を前提としてよい。
- **表紙画像・出版情報の補完**: openBD API（ISBN指定）。APIキー不要
  - **（タスク10で確認）出版日フィールドの扱い**: openBDの`summary.pubdate`は書誌によって`YYYY`/`YYYYMM`/`YYYYMMDD`のいずれかの桁数で返ってくることを実測で確認済み（固定フォーマットではない）。また表紙画像が無い書誌では`summary.cover`が空文字列で返る（HTTPエラーにはならない）。`lib/openbd.ts`の`getBookInfoByIsbn`は、これらの空文字列を`null`に正規化して返すが、`publishedDate`自体は日付型に変換せず生の文字列のまま返す。以降、この値を画面表示・保存に使うタスク（新規登録フォーム等）では、この可変長フォーマットを前提に表示用の整形処理を別途行うこと（`books`テーブルの`finished_date`とは別物であり、混同しないよう注意する）。
- **おすすめ本の生成**: Anthropic Claude API。記録済みの本の一言要約・評価・タグを整形してプロンプトを組み立て、まだ読んでいなさそうな本を提案してもらう
- **認証**: アカウント登録なしの単一パスワード方式。環境変数 `APP_PASSWORD` と比較し、一致すれば署名付きCookieを発行する。Next.jsの`proxy.ts`（後述4-2-3参照。旧`middleware.ts`に相当）で、Cookie未検証のリクエストを `/login` にリダイレクトする（`/login` と `/api/login` 自体は除外）

### 4-2-3. このNext.jsバージョン（16系）固有の注意: `middleware.ts`ではなく`proxy.ts`を使う

このプロジェクトが使うNext.jsのバージョンでは、`middleware`ファイル規約が`proxy`に名称変更されている（`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md`参照）。**`middleware.ts`という名前のファイルを作ると、非推奨のEdge Runtimeで動作し、Node.js標準の`crypto`モジュール等が使えずクラッシュする**（実際にタスク8でこの事象が発生し、`proxy.ts`へのリネームで解決した）。

- ログイン状態のチェック・リダイレクト・APIの認証ガードといった役割を持つファイルは、必ず`proxy.ts`という名前で作り、エクスポートする関数名も`proxy`にすること
- `export const config = { runtime: ... }`のような`runtime`オプションはproxyファイルではサポートされておらず、指定するとエラーになる。何も指定しなければNode.js実行環境がデフォルトになるので、`node:crypto`等はそのまま使える
- 以降のタスクで`proxy.ts`に変更を加える場合も、このファイル名・関数名を維持すること
- **相関図の描画方法**: `d3-force`（力学シミュレーションのみを提供する軽量ライブラリ。フルセットのd3やreact-flowのような重量級UIライブラリは使わない）でノード（本）の配置を計算し、結果をReactコンポーネント内で素のSVG（`<circle>`/`<line>`）として描画する

### 4-2-1. Supabase接続情報の使い分け

- `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`: クライアント側（ブラウザ）から読む場合に使う
- `SUPABASE_SERVICE_ROLE_KEY`: APIルート（サーバー側）でのみ使う。クライアントに露出させないこと

### 4-2-2. 未認証時のproxyの応答方針（タスク8で確定）

`proxy.ts`は、未認証（Cookie無し、または署名・期限切れが無効）の場合、パスの種類によって応答を変える。

- **ページルート**（`/login`以外の通常の画面）: `/login`へリダイレクト
- **`/api/`から始まるパス**（`/api/login`を除く）: リダイレクトせず、JSON形式の401エラー応答を返す（`NextResponse.json({ error: "..." }, { status: 401 })`）。理由: APIルートはfetchで呼ばれる想定であり、fetchは既定でリダイレクトを追跡するため、JSONを期待するクライアント側がログイン画面のHTMLを受け取って壊れることを避けるため
- `/login`, `/api/login`自体は認証チェック対象外

以降のAPIルート実装タスク（12〜17）・UI実装タスク（18〜25）は、この応答方針を前提にしてよい（APIルートは401時にJSONを返せば十分で、独自にリダイレクト処理を書く必要はない。UI側でfetchが401を受け取った場合の扱いが必要な画面では、これを踏まえて実装すること）。

### 4-3. ディレクトリ構成（目安）

```
app/
  page.tsx                本の一覧画面
  books/[id]/page.tsx     本の詳細・編集画面
  books/new/page.tsx      本の新規登録画面
  graph/page.tsx          本のつながりの相関図画面
  recommend/page.tsx      おすすめ本画面
  login/page.tsx          ログイン画面
  api/
    books/route.ts
    books/[id]/route.ts
    links/route.ts
    book-search/route.ts
    recommend/route.ts
    login/route.ts
    logout/route.ts
lib/
  supabase.ts             Supabaseクライアント初期化
  db/books.ts             booksテーブルへのCRUD
  db/links.ts             book_linksテーブルへのCRUD
  auth.ts                 パスワード比較・Cookie発行/検証
  bookSearch.ts           NDL Searchでのキーワード検索
  openbd.ts               openBDでの表紙・出版情報取得
  recommend.ts            Claude APIへのプロンプト組み立て・レスポンス解析
types/
  book.ts                 本・リンクの型定義
proxy.ts                  認証ガード（Next.js 16の規約名。4-2-3節参照）
```

### 4-4. データモデル（Supabase / PostgreSQL）

- `books`: `id`, `title`, `author`, `publisher`, `isbn`, `cover_url`, `rating`(1〜5), `one_line_summary`, `content_summary`, `why_resonated`, `how_to_apply`, `surprising_point`, `quote`, `tags`(text配列), `finished_date`, `created_at`
- `book_links`: `id`, `from_book_id`, `to_book_id`, `note`（関連の一言説明、任意）, `strength`（関連度、smallint 1〜3、デフォルト2。タスク29以降で追加）。片方向で保存し、表示・相関図生成時には双方向として扱う

### 4-5. 命名規則・テストコマンド

- ファイル名はNext.jsの規約（`page.tsx`, `route.ts`）に従う。それ以外のモジュールはcamelCase
- 型チェック: `npx tsc --noEmit`
- ビルド確認: `npm run build`
- 単体テスト: Vitestを使用し `npm test` で実行する

### 4-6. 過去プロジェクトからの適用教訓

- npm installのpostinstallスクリプトはnode本体をPATH経由でしか解決できない — この開発環境ではnode/npmがデフォルトPATHに無い場合があるため、`npm.cmd`のフルパス指定だけでなくnode本体も解決できるようPATHを整えてからインストールする
- 呼び出し内で重複した値を含む配列からの複数INSERTは事前に重複除去する — `book_links`やタグのような多対多・配列データをまとめて書き込む処理では、事前にSet化するなどして重複値によるUNIQUE制約違反を避ける
- PaaSへのGitHub連携pushだけでは自動再デプロイの完了を確認したことにならない — Vercelへのデプロイに関わるタスクでは、pushしただけで終わらせず、公開URLへの実アクセスで反映を確認する（未反映なら人間に手動デプロイを依頼する）
- フロントエンドタスクの検証方法に「ブラウザでの手動操作確認」を安易に指定しない — この環境には対話的ブラウザ操作ツールが無いため、構文チェック・`curl`/`fetch`でのAPI疎通確認・コードレビューで代替する
- 非同期処理中はボタンを非活性化しローディング状態を示す — 「おすすめ本を提案」ボタンなど、外部APIを呼び出す処理は数秒かかりうるため、処理中はボタンをdisabledにしローディング表示を出す
- Zod v4では一部のAPIが非推奨になっている — `z.string().url()`ではなく`z.url()`を、`.flatten()`ではなく`z.treeifyError()`を使う

### 4-7. 再発防止チェックリスト

以下は複数の過去プロジェクトで独立に再発が確認された教訓です。計画提示（第3章3-1）の「再発防止チェック」で、各項目について「該当する/しない」と対応方針を必ず回答してください。

- [ ] タップ・クリック可能な要素（ボタン、アイコン、リンク等）は、実効タップ領域が最低44×44px相当になっているか

### 4-8. デプロイ後の追加改善（人間の使用感フィードバックに基づく、タスク29〜39）

タスク1〜28完了・Vercelへのデプロイ・人間による実機確認を経て、人間から追加の改善アイデアが寄せられた。以下は親AIが事前に決定した設計方針であり、各タスクの担当エージェントはこれを自己判断で変更しないこと。

- **一覧カード・相関図ノードへの表紙画像**: `book.cover_url`がある場合のみ表示する。無い場合は現状通り画像枠を出さない（プレースホルダー画像は用意しない、YAGNI）。一覧カードは左側に48×64px程度のサムネイル（素の`<img>`、`object-cover`）。相関図ノードはSVGの`<clipPath>`で円形にクリップして表示する。
- **一覧グリッドの調整**: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`に加え`xl:grid-cols-4`を追加する。登録冊数が少ない場合にカードが左に寄って見えるのはgridレイアウトの通常の性質であり不具合ではないため、件数が少ない時だけ中央寄せにするような特殊分岐は追加しない。
- **相関図の関連度（strength）**: `book_links`に`strength`列（smallint、1〜3、デフォルト2＝普通、CHECK制約で1〜3に限定）を新設する。リンク作成UIで「弱い/普通/強い」を選択可能にする（デフォルト「普通」）。相関図の線はstrengthに応じて太さ・色を変える（1: 細い薄灰、2: 中間、3: 太い濃色）。forceLinkの`distance`（ノード間の距離計算）は変更しない（過剰実装を避けるため、視覚表現は線の太さ・色のみに留める）。
- **おすすめ本のスケルトンローディング**: ボタン押下後、結果カード3枚分のTailwind `animate-pulse`によるグレーのアニメーションプレースホルダーを表示する。既存の「考え中…」というボタンのテキスト表示は変更せずそのまま残し、スケルトンは追加で表示する（置き換えではなく併用）。

---

## 5. 最初のタスク

### タスク1: Next.jsプロジェクトの初期化

Next.js（App Router、TypeScript）のプロジェクトをこのディレクトリ直下に初期化してください。

- `npx create-next-app@latest` 相当の構成で、TypeScript・ESLintを有効にする（Tailwind CSSの採用可否は任せます。採用する場合はその理由を完了報告に一言添えてください）
- `.gitignore` に `.env`, `.env.*`, `node_modules/`, `.next/` を含める
- 4-3節のディレクトリ構成を見据えて、`lib/`, `types/` の空ディレクトリ（またはプレースホルダファイル）を用意する
- README等の生成物はそのままで構いません

**検証方法**: `npm run build` が成功すること。`npm run dev` でローカルサーバーを起動し、Node.jsの`fetch`でトップページ（`http://localhost:3000`）にリクエストを送り、200が返ることを確認する。
