# summary.md — book-notes-app プロジェクト完了サマリ

## 概要

個人用読書ノートアプリ（Next.js App Router + Supabase + Vercel）。当初計画（タスク1〜28）に加え、デプロイ後の人間フィードバックに基づく追加改善（タスク29〜39、相関図の関連度・表紙画像・スケルトンローディング等）を実施。全タスク完了、Vercelへのデプロイ・人間による本番環境での実機確認まで完了した。

## 知識統合結果

`session_log.md`（タスク1〜39・本番確認）・`human_feedback.md`を通しで読み返し、以下をトリアージした。

### 新規採用（knowledge-base/stacks/nextjs.md、新規作成）

- middlewareの動作確認はユニットテストだけでなく実際の`npm run dev`起動で行う（タスク8由来）
- App Routerの`app/**/layout.tsx`は予約ファイル名であり、無関係なロジックファイルに使えない（タスク24由来）
- Reactの派生状態は`useEffect`+`setState`ではなくレンダー中の計算で表す（タスク21・23由来）
- d3-forceによるノード配置計算は、Reactコンポーネントから分離した決定論的な純粋関数として実装するとテスト可能になる（タスク24由来）

### 新規採用（knowledge-base/stacks/typescript.md）

- Zodで「空文字列をundefinedに変換する」設計は、部分更新APIと組み合わせると値をクリアできなくなる（タスク22の重大バグ発見由来）

### 新規採用（knowledge-base/general.md）

- 全画面が出そろった時点で、画面間の導線に抜けが無いかまとめて点検する（タスク25、およびタスク18・14と合わせて同一プロジェクト内で3回発生した抜けパターンを一般化）
- 人間に同じ手動操作（SQL実行等）を複数回依頼する場合は、前回の入力が残っていないか確認するよう明記する（タスク30由来）

### 再確認（既存エントリの裏付け強化）

- フロントエンドタスクの検証方法に「ブラウザでの手動操作確認」を安易に指定しない（タスク38、実装着手前に指摘し修正）
- PaaSへのGitHub連携pushだけでは自動再デプロイの完了を確認したことにならない（タスク27・追加改善デプロイ双方で独立確認を実施、Vercelのデプロイ切り替え中の一時的404も観測）
- Windows環境でのcurlによる日本語ペイロード送信は信頼できないことがある（タスク39）
- タップ/クリック可能領域は最低44×44px相当を確保する（タスク34、計画段階の誤判定を実装前に是正）

### 破棄（単発の見落としで技術的教訓化しないもの）

- タスク33で発生した、検証後のdevサーバーPID終了忘れ（既存の「PID指定で個別終了」というルール自体は健在で、単発のオペレーションミスと判断）

## プロトコル運用上の所感

- 2段階ゲート制（計画提示→承認→実装）は、タスク32・34・38で計画段階の不備（チェック項目の記載漏れ、誤判定、実行不可能な検証方法の指定）を実装前に是正でき、想定通り機能した。
- タスク22（編集フォームでの重大なデータ消失バグ）・タスク25（ナビゲーション抜け）の発見は、いずれも親AI自身の追加境界チェック（検証コマンドの再実行だけに頼らない）によるものであり、1-3節の設計思想が実際に機能した実例となった。
